# Agent context

Long-lived context, briefings, and references for agents working in this repo.

Use this directory for material an agent should read to get oriented:
project goals, architectural notes, design rationale, links to external
sources of truth, etc.

For ephemeral working files (plans, scratch scripts, in-progress notes),
use `/temp/` per `AGENTS.md`.
